# AGENTS-CN.md — godot-td

## 项目信息
- **引擎：** Godot 4.7.1-stable，安装路径为 `D:\Godot_v4.7.1-stable`。
- **语言：** GDScript。
- **类型：** 塔防游戏。
- **状态：** 已实现第一版可玩版本。项目文件、自动加载、基础场景以及资源/经济框架均已存在。

## 已确认引擎信息
- 引擎可执行文件路径：`D:\Godot_v4.7.1-stable\Godot_v4.7.1-stable_win64.exe`。
- 执行项目命令或打开编辑器时必须使用上述路径，不要假设 `godot` 已在 `PATH` 中。

## 游戏设计约束（来自产品需求）
- **出兵点：** 地图左上角、中上、右上角。
- **基地：** 地图正下方。
- **敌人移动：** 沿固定道路按预设路线移动。
- **敌人 buff：** 0 个或多个，包括防御加成、法抗加成、移速加成、血量加成。
- **敌人成长：** 攻击力随游戏难度和波次增加而提高。
- **玩家角色：** 可放置在地图上，通过击杀敌人获得经验值并升级（最高 10 级）。
- **角色等级节点：** 3、5、8 级时各解锁一个技能。
- **每次解锁的技能类型：** 物理攻速、物理攻击力、法术攻击力、法术冷却缩减、法术技能激活。
- **装备系统：** 角色可以装备武器。
- **角色进阶：** 角色可消耗金钱升级；进阶后获得新称号，并从 1 级重新开始，3、5、8 级仍会解锁新技能。
- **资源：** 装备生产、木材采集、矿石采集、金币收集。
- **胜利条件：** 击败所有波次敌人。
- **失败条件：** 敌人摧毁玩家水晶。

## 本仓库开发规范
- 使用 GDScript 2 语法（Godot 4.x）。
- 遵循 Godot 场景与脚本同放约定：每个场景的 `.tscn`、`.gd` 及相关资源放在清晰的文件夹下（例如 `scenes/enemies/`、`scenes/towers/`、`autoload/`、`resources/`）。
- 共享数据（buff、技能、敌人波次、称号等）使用 `Resource` 类或 `JSON` 数据文件定义，方便策划调整数值而无需修改代码。
- 优先使用 `Node` 组合，避免过深的继承层级。

## 推荐初始化步骤
如果项目尚未创建：
1. 使用上述引擎可执行文件创建 `project.godot`。
2. 添加自动加载单例 `GameManager`，管理全局状态（波次、金钱、木材、矿石、难度）。
3. 添加自动加载单例 `PathManager`，管理预烘焙敌人路线。
4. 创建基础场景：`Main`、`Map`、`Enemy`、`Tower`、`Projectile`、`BaseCrystal`、`WaveSpawner`、`UIRoot`。

## 素材
- **外部素材根目录：** `D:\projects\godot-td\material\`。
- 项目现在使用以下分类的 Kenney CC0 素材包：
  - `tower-defense-top-down/` — 坦克单位，用作移动敌人。
  - `kenney_ui-pack/` — 按钮、面板、箭头等 UI 元素。
  - `kenney_ui-audio/` — 点击、悬停等 UI 音效。
  - `kenney_rpg-audio/` — 攻击、爆炸等游戏音效。
  - `kenney_smoke-particles/` — 烟雾/爆炸粒子贴图。
- 部分场景仍使用彩色多边形占位；替换美术时从上述文件夹中选取精灵。

## 验证方式
- 通过上述引擎可执行文件打开项目。
- 使用 `F5`（运行）或在命令行执行：`Godot_v4.7.1-stable_win64.exe --path <项目根目录>`。
- 目前尚无测试；如有自动化测试需求，可添加 `Gut` 插件或使用 Godot 内置测试运行器。
